# LSEMB Python Services Dependencies
# Python 3.10+ required

# Core Web Framework
fastapi==0.115.0
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Async Support
aiohttp==3.10.5
# asyncio==3.4.3  # REMOVED: stdlib on 3.10+; the PyPI backport (2015) shadows & breaks stdlib asyncio

# Web Scraping - Crawl4AI (OPTIONAL — import guarded in crawl4ai_service.py)
# REMOVED from the default install: crawl4ai 0.3.0 pins lxml==5.3.0 + numpy==2.1.1 which conflict
# with pandas/pgvector (numpy<2), and it is absent on the air-gapped GX10 appliance. Re-add in a
# crawler-enabled build: crawl4ai==0.3.0 (and bump lxml/numpy accordingly).
# crawl4ai==0.3.0

# Database
psycopg[binary,pool]==3.1.13
psycopg2-binary==2.9.12  # SQLAlchemy / APScheduler SQLAlchemyJobStore driver (psycopg3 above is async-side)
asyncpg==0.29.0
pgvector==0.2.3
neo4j==5.28.0

# pgai - Automatic Embeddings (will be installed separately)
# pgai==0.2.0

# Redis for Queue/Cache + RedisVL (semantic cache / vector — Redis Iris OSS path)
# NOTE: code imports `redis.asyncio as aioredis` (modern redis-py); the standalone
# aioredis package was unused and is dropped. redis pinned <6 to satisfy celery/kombu 5.3.4,
# >=5.1 for the Redis 8 Query Engine (FT.*) commands RedisVL relies on.
redis>=5.1,<6  # resolution-verified to redis-5.3.1; <6 keeps celery/kombu 5.3.4 happy
redisvl==0.19.0  # SemanticCache = Iris "LangCache" OSS equivalent (no torch pulled — reuse existing embed path)

# Message Queue
celery==5.3.4
kombu==5.3.4

# Job Scheduling (APScheduler)
apscheduler==3.10.4
croniter==2.0.1
sqlalchemy==2.0.23

# Google Drive API
google-auth==2.25.2
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client==2.111.0

# Data Processing
pandas==2.1.3
numpy==1.26.2
polars==0.20.3
beautifulsoup4==4.12.3
lxml==4.9.3
PyPDF2==3.0.1

# LLM Support
openai>=1.50.0
anthropic>=0.35.0
google-generativeai>=0.8.0
# REMOVED: langchain is not imported anywhere in the service; the loose pin resolved to
# langchain 1.x which requires pydantic>=2.7.4, conflicting with pydantic==2.5.2 below.
# langchain>=0.3.0
# langchain-openai>=0.2.0

# Speech-to-Text - OpenAI Whisper (DISABLED: Heavy dependencies)
# openai-whisper==20231117
# torch==2.1.1
# torchaudio==2.1.1
# ffmpeg-python==0.2.0
# openai>=1.0.0  # For Whisper API mode

# YouTube Support
yt-dlp==2024.8.6

# API Documentation
pydantic==2.5.2
python-jose[cryptography]==3.3.0

# Utilities
python-dotenv==1.0.0
httpx==0.27.2
tenacity==8.2.3
loguru==0.7.2

# Reranking (uses httpx for direct Jina API calls, no extra package needed)
# httpx already included in Utilities section

# SSH & DevOps
paramiko==3.4.0
cryptography>=41.0.0

# OCR / Vision (PDF -> image, image OCR) — top-level imported by the *_ocr / pdf_vision services
Pillow==12.2.0
PyMuPDF==1.27.2.3        # provides `fitz`
pytesseract==0.3.13      # needs the system `tesseract` binary at runtime (not for import)

# Monitoring
prometheus-client==0.19.0
psutil==7.2.2            # health_router system stats (CPU/mem/disk)

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Development
black==23.11.0
pylint==3.0.3
mypy==1.7.1