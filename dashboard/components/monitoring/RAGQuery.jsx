import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const RAGQuery = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>RAG Query</CardTitle>
      </CardHeader>
      <CardContent>
        <p>RAG query component placeholder.</p>
      </CardContent>
    </Card>
  );
};

export default RAGQuery;