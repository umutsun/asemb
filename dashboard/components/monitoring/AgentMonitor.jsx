import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const AgentMonitor = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Agent Monitor</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Agent monitoring component placeholder.</p>
      </CardContent>
    </Card>
  );
};

export default AgentMonitor;