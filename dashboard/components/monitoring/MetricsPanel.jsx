import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const MetricsPanel = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Metrics Panel</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Metrics panel component placeholder.</p>
      </CardContent>
    </Card>
  );
};

export default MetricsPanel;